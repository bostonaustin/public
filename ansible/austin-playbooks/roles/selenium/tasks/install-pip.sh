#!/usr/bin/env bash
# run the pip cmd via script since 'pip' module can't locate requirements

#/usr/local/bin/pip install -r ${ANSIBLE_HOME}/austin-playbooks/roles/selenium/tasks/requirements.txt
/usr/local/bin/pip install -r /media/psf/Home/Dropbox/austin-playbooks/roles/selenium/tasks/requirements.txt